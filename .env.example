# ---------------------------------------------------------------------------
# MealMesh environment variables
# Copy this file to `.env` and fill in the values. `.env` is gitignored.
# ---------------------------------------------------------------------------

# Supabase — CLIENT-SAFE public values.
# The `EXPO_PUBLIC_` prefix exposes these to the app bundle (this is expected;
# Row Level Security, not secrecy, protects the data).
# Find them in: Supabase Dashboard → Project Settings → API.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Food photos (optional) — Pexels API key (free at pexels.com/api). When set,
# recipe cards show real photos; otherwise they show emoji tiles.
# EXPO_PUBLIC_PEXELS_KEY=

# Analytics (added later) — Mixpanel project token (client-safe).
# EXPO_PUBLIC_MIXPANEL_TOKEN=

# Blog (separate Astro site, see /blog) — once deployed, set this so the
# "Articles" nav link and the "From the kitchen" strip point at real posts
# instead of falling back to the in-app placeholder section.
# EXPO_PUBLIC_BLOG_URL=https://blog.getmealmesh.com

# Web subscriptions — Freemius PUBLIC identifiers (client-safe).
# Find in the Freemius dashboard: Product → Settings.
# Three regional Pro plans, one Plan ID each — see src/lib/pricing.ts for the
# country -> tier rule. EXPO_PUBLIC_FREEMIUS_PLAN_ID_GLOBAL is required;
# the other two fall back to it if unset.
# EXPO_PUBLIC_FREEMIUS_STORE_ID=               # Store ID (dashboard URL: .../stores/<id>/...) — powers the customer portal link
# EXPO_PUBLIC_FREEMIUS_PRODUCT_ID=             # Product ID
# EXPO_PUBLIC_FREEMIUS_PLAN_ID_PK=             # Pakistan-tier plan's ID ($2.99/mo, $24.99/yr)
# EXPO_PUBLIC_FREEMIUS_PLAN_ID_ASIA_AFRICA=    # Asia/Africa-tier plan's ID ($5.99/mo, $48.99/yr)
# EXPO_PUBLIC_FREEMIUS_PLAN_ID_GLOBAL=         # global-tier plan's ID ($9.99/mo, $99.99/yr)
# EXPO_PUBLIC_FREEMIUS_PUBLIC_KEY=             # Keys → Public Key (pk_...)

# ---------------------------------------------------------------------------
# SERVER-ONLY SECRETS — do NOT put these here or anywhere in the app/repo.
# Set them in the Supabase Edge Function secrets instead:
#   Dashboard → Edge Functions → Secrets  (or `supabase secrets set ...`)
#
#   GEMINI_API_KEY            ← your Gemini API key, free at aistudio.google.com/apikey
#                                (NEVER ships to the client)
#   GEMINI_MODEL              ← optional, defaults to "gemini-2.5-flash" — set this to
#                                try a different free-tier model without a redeploy
#   FREEMIUS_SECRET_KEY       ← Freemius Keys → Secret Key (sk_...), for the webhook
#   SUPABASE_SERVICE_ROLE_KEY ← injected into functions automatically
# ---------------------------------------------------------------------------
