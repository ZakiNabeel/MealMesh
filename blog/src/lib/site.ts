// The main app lives on a separate Vercel project/domain. Configure via
// PUBLIC_APP_URL once it's live; this default matches the domain used
// elsewhere in the MealMesh planning docs.
export const APP_URL = import.meta.env.PUBLIC_APP_URL || 'https://getmealmesh.com';
export const SITE_NAME = 'MealMesh';
export const SITE_TAGLINE = 'One household. Many diets. One plan.';
